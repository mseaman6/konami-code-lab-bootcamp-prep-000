const code = [38, 38, 40, 40, 37, 39, 37, 39, 66, 65]

function init() {
  const body = document.body;
  body.addEventListener('keydown', function (e) {
    var keysEntered = [];
    keysEntered = keysEntered + e.which
    if(e.which ===
  })

}